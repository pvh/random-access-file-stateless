{
  "name": "random-access-file-stateless",
  "version": "1.0.0",
  "description": "create new file handles on every request for random-access-file to avoid hitting EMFILE",
  "main": "index.js",
  "repository": "https://github.com/pvh/random-access-file-stateless",
  "author": "Peter van Hardenberg <pvh@pvh.ca>",
  "license": "MIT",
  "scripts": {
    "test": "node test.js"
  },
  "dependencies": {
    "random-access-file": "^2.1.3",
    "random-access-storage": "^1.4.0"
  },
  "devDependencies": {
    "random-access-test": "https://github.com/random-access-storage/random-access-test"
  }
}
